query currentUser {
  currentUser: me {
    ...User_
  }
}

fragment User_ on User {
  ...UserNoNested
}

fragment UserNoNested on User {
  id
  name
  data
}

mutation createToken {
  createAuthToken {
    token
    User {
      ...User_
    }
  }
}
